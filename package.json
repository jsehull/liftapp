{
  "name": "lift-app",
  "version": "0.3.0",
  "description": "weight and rep history per exercise",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "node-sass styles.scss styles.css",
    "sass-watch": "node-sass -w styles.scss styles.css",
    "serve": "http-server -o",
    "dev": "parallelshell --kill-others \"npm run sass-watch\" \"npm run serve\"",
    "build": "npm run sass && npm run serve"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@bitbucket.org/yellowslide4/liftapp.git"
  },
  "author": "Jesse Hull <jse.hull@gmail.com>",
  "private": true,
  "devDependencies": {
    "http-server": "^0.9.0",
    "node-sass": "^3.7.0",
    "parallelshell": "^2.0.0"
  }
}
